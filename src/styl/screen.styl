@import "pages/hello.styl"
@import "pages/exercise.styl"

#app {
  font-family: 'Avenir', Helvetica, Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  color: #2c3e50;
}

#nav
  text-align center
  padding 2rem

  a
    font-weight: bold;
    color: #2c3e50;

    &.router-link-exact-active
      color: #42b983;

.home
  display flex
  flex-direction column
  align-items center
